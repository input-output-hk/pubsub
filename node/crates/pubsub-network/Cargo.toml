[package]
name = "pubsub-network"
description = "Network layer implementations: Cyclon, Vicinity, Hybrid Dissemination, QUIC transport"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
pubsub-types = { path = "../pubsub-types" }
tokio = { workspace = true }
quinn = { workspace = true }
rustls = { workspace = true }
serde = { workspace = true }
ciborium = { workspace = true }
pallas-crypto = { workspace = true }
rand = { workspace = true }
tracing = { workspace = true }
dashmap = { workspace = true }
bytes = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
rcgen = "0.14"
serde_json = { workspace = true }

[features]
cardano = ["dep:pallas", "dep:reqwest", "dep:hex"]
# Enable MockNodeRegistry and the NodeRegistry trait (replication servers, D2 Ch.4).
replicate = ["pubsub-types/replicate"]

[dependencies.pallas]
version = "0.35"
optional = true

[dependencies.reqwest]
version = "0.13"
features = ["json"]
optional = true

[dependencies.hex]
version = "0.4"
optional = true
