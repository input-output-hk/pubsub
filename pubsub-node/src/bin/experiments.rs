//! Experiments driver binary. The command-line front end lands with the
//! sweep configuration work; until then this is a placeholder.

fn main() {
    eprintln!("experiments: no front end yet");
    std::process::exit(1);
}
