# Tier-1 comparison cell — M5 N=4000 (6,3): P(bad) pred 0.204, MC 285/1500 = 0.190 (swap symmetry pair).
# mu = 0.2; run count matched to the formal MC row (or sized for the bulk value).

model = "m5"
master_seed = 508

[population]
size = 4000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 6
publisher = { pick_count = 3 }
fanout = "forward-to-all"

[strategies.adversarial]
pick_count = 6
publisher = { pick_count = 3 }
fanout = "silent-relay"

[execution]
runs_per_experiment = 1500
