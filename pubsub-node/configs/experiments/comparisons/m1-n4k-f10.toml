# Tier-1 comparison cell — M1 N=4000 F=10: P(bad) pred 0.655, MC 660/1000 = 0.660.
# mu = 0.2; run count matched to the formal MC row (or sized for the bulk value).

model = "m1"
master_seed = 510

[population]
size = 4000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 0
publisher = { pick_count = 10 }
fanout = "forward-to-all"

[strategies.adversarial]
pick_count = 0
publisher = { pick_count = 10 }
fanout = "silent-relay"

[execution]
runs_per_experiment = 1000
