# Tier-2 comparison cell — M1 N=4000 F=14: P(bad) pred 0.0420, MC 165/4000 = 0.0413.
# mu = 0.2; run count matched to the formal MC row.

model = "m1"
master_seed = 605

[population]
size = 4000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 0
publisher = { pick_count = 14 }
fanout = "forward-to-all"

[strategies.adversarial]
pick_count = 0
publisher = { pick_count = 14 }
fanout = "silent-relay"

[execution]
runs_per_experiment = 4000
