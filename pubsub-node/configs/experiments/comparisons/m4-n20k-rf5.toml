# Tier-2 comparison cell — M4 N=20000 RF=5: P(bad) pred 0.0893, MC 749/8000 = 0.0936.
# mu = 0.2; run count matched to the formal MC row.

model = "m4"
master_seed = 607

[population]
size = 20000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 5
symmetric = true
fanout = "forward-to-relays"

[strategies.adversarial]
pick_count = 5
symmetric = true
fanout = "silent-relay"

[execution]
runs_per_experiment = 8000
