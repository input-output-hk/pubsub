# Tier-1 comparison cell — M1 N=20000 F=12: P(bad) pred 0.661, MC 268/400 = 0.670.
# mu = 0.2; run count matched to the formal MC row (or sized for the bulk value).

model = "m1"
master_seed = 512

[population]
size = 20000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 0
publisher = { pick_count = 12 }
fanout = "forward-to-all"

[strategies.adversarial]
pick_count = 0
publisher = { pick_count = 12 }
fanout = "silent-relay"

[execution]
runs_per_experiment = 400
