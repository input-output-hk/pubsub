# Cost/latency/degree comparison cell — M1 operating point F=24 (307202 msgs, 19.2 copies, hops 5.0 / 3.6).
# N = 20000, mu = 0.2; 200 runs (the published values were measured over
# 40-200 graphs per cell).

model = "m1"
master_seed = 704

[population]
size = 20000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 0
publisher = { pick_count = 24 }
fanout = "forward-to-all"

[strategies.adversarial]
pick_count = 0
publisher = { pick_count = 24 }
fanout = "silent-relay"

[execution]
runs_per_experiment = 200
