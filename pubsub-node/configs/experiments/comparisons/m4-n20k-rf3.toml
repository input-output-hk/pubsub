# Tier-1 comparison cell — M4 N=20000 RF=3: P(bad) pred 1.000, MC 2000/2000.
# mu = 0.2; run count matched to the formal MC row (or sized for the bulk value).

model = "m4"
master_seed = 501

[population]
size = 20000
adversarial_fraction = 0.2
topic = "t0"

[strategies.honest]
pick_count = 3
symmetric = true
fanout = "forward-to-relays"

[strategies.adversarial]
pick_count = 3
symmetric = true
fanout = "silent-relay"

[execution]
runs_per_experiment = 200
