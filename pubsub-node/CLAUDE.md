<!-- SPECKIT START -->
**Feature roadmap (read first if planning the next feature)**: `specs/ROADMAP.md` — feature list 002–012 with architectural anchors (edge-vs-golden config split, connection-direction inversion) and per-feature open questions. **Feature numbers are IDs, not a strict build order** — see the note at the top of ROADMAP.

**Active work**: branch `experiments-m4-synthesis` (E19 merged 2026-08-18 via PR #177; `origin/main` merged back in — the branch is PR-ready). The M4 synthesis pass toward the CIP recommendation: the (N, K)-parameterised prediction ledger `docs/experiments/m4_synthesis_predictions.py` (the E18/E19 forms lifted to parameters; directional + M3 cross-seam forms validated 7-for-7 against E10's measured points and both published ungated op points; `capsweep` + the gated model `compare`); **eleven pre-registered cells, seeds 1139–1149, every one verified against its frozen registration**. Headlines: gated **K = 10, B = 500, C = 23 is equal-or-better than the ungated CIP op point on every quoted axis** (P(bad) 5.1e-6 vs 6.1e-6, copies/honest 13.00 vs 13.40 — the shared pool absorbs the extra pick, hops 5.00/3.95, churn budget 7.57 % vs 7.43 %) with flood divisor 500 and degree ≤ 33 exact; the gated K = 9 row measured (6× headroom cost, churn 2.65 %); the c-floor rehearsal validated the composition form at K = 9 (c ≈ 3.5 floor, the C = 20 mid-slope point conservative); the **equal-surface cliff pair** measured M3-vs-M4 at surface 32 (M3 17/400 bad, all deaf-class as dissected vs M4 400/400 — the pair draw gets twice the pool per unit of attack surface, no M3 pick count feasible at M4-equal surface); the **N-041 completion** (publisher detail slot pair at `048457f`, seven sweeps byte-identical) enabled the pubseam cells, which found the **seed-rescue coupling** (a binding seed-intake cap starves exactly the rescuing seeds, f = μ + (1−μ)ρ_p — size C_p to clear the intake load) and **fired N-042's trigger** (rank dissection reproduced the signature; fix stays parked, charter written). Report committed (`docs/experiments/m4-synthesis.md` — §9 catalogues the gated closed forms with grades + validation records); the gated comparison artifact + groundwork/followups in `notes/`. Next: this branch's PR → the formal-team review of §9's forms → the instrument pass (per-victim arrival order + failure-severity batch, charter + frozen validation re-runs in `notes/m4-synthesis-followups.md`).

**Merged dependencies on `main`** (newest first):

- **E19 (merged 2026-08-18, PR #177)** — **symmetric flooding under the admissions budget + the ordered comparison arm** (E18's benefit side; fires and resolves N-032 + N-040; ADRs **0042** (the symmetric acceptance cap is an admissions budget) and **0043** (the ordered symmetric comparison predicate); no Spec Kit — the PR #138/#152 combined machinery-plus-pass pattern). **Machinery**: drain-time route attribution in the detail rows (own-only/mutual/admitted × linked-peer class, refusals split fresh vs crossing; run rows untouched — no re-baseline), the **admissions-budget semantics** (a per-(topic, kind) admitted-count folded at the symmetric admission site read by symmetric `UnifiedAcceptance` instances instead of the both-role link scan; crossings short-circuit ahead of gate and cap; the Epoch fold refunds; realised degree ≤ K + C by construction), and the `symmetric_ordered` experiments-config coordinate (`edge-sym-ordered/v1`, requires `symmetric`, no CLI knob). **Cells**: 27 over seeds 1112–1138 (N = 4 000, K = 16; per-cell predictions frozen in config comments before running; means as run-clustered mean ± SE, probabilities as counts + Wilson), report `docs/experiments/symmetric-flooding.md` + `symmetric_flooding_predictions.py` + `summarise_symmetric_flooding_cell.py`. **Results**: the retired both-role scan measured once at its pinned pre-ADR commit (crossing veto 0.074/victim; the budget's veto ≡ 0 over ~10⁷ refusals; admitted Sybil composition scheme-independent at 41–42 %); the pilot-calibrated **without-replacement race law at zero flags across the 19-cell B × cap × Sybil grid** (every |z| ≤ 2.6; admitted ≤ C and degree ≤ K + C exact everywhere); the occupancy decomposition — the **cap-blind own-pick floor K·μ** no acceptance policy sees + the gate-divided admission route (S/B)(1−m); cap sizing re-anchored on the fresh-arrival load K(1−m)(1−μ) and shown **predicate-dependent**; flooding structurally inert at pool saturation; the **cap × empty-pool composition term, replicated** (pooled 166/800 bad vs the uncapped law's 0.148, z = +4.75; the first-order form computed across the whole cap ladder — the ledger's `capsweep` — and measured at both ends: C = 3 at P(bad) 0.196 predicted/0.208 measured, the pre-registered C = 12 quiet-end anchor at 59/400 vs registered 59.3, z = −0.04; the uncapped anchor byte-re-attested at HEAD) — past the pool floor no cap both binds and stays harmless, inside the window the term is doubly suppressed *given* the sizing rule; the **ordered arm measured**, completing E18 §4 (pick-repair real only below the saturation boundary B < (N−1)/K: 0/8 000 at equal B vs the unordered 57; 80/8 000 ≈ the unordered cliff at equal density — the pre-registration corrected the follow-up note's own expectation; at a binding budget the 2/B looseness becomes doubled honest starvation while occupancy clamps) — N-039's rejection is now measured dominance. **Instrument**: N-042 recorded (the wavefront budget race is class-fair but rank-concentrated dialer-side — the ordered flooder's 386/400 coverage row so attributed; fix = per-victim seeded order, parked for the next re-baseline generation with the failure-severity batch); byte chain `23d0223`/`21acd36` → (E13 fields only) → `b294391` → `9197868` → `9865b96` → `6d0385b` exact on all seven sweeps, plus the gated-symmetric path attested separately (E18's μ = 0.4 cell byte-identical at HEAD — §6's comparison is same-instrument). Next after merge: the synthesis pass (E10 + E12 + E18 + E19 → the CIP M4 recommendation) carrying the CIP-scale anchor cells.

- **E18 — gated-symmetric selection (gated M4)** (merged 2026-08-15, PR #174; no Spec Kit, config-only on the existing machinery; N-039's revisit trigger, fired). Eleven cells in `configs/experiments/gated-symmetric/` (seeds 1101–1111; N = 4 000, K = 16; μ = 0.2 plus the μ-axis pair at 0.3/0.4), report `docs/experiments/gated-symmetric.md` + the μ-parameterised exact-arithmetic `gated_symmetric_predictions.py` — predictions recorded before any cell ran, **eleven for eleven on the ledger**. Results: the shared-pool degree law d = λm(2−m) to three digits across B = 10…500 (**no plateau** — the pair draw puts B in the degree everywhere; mutual-pick overlap ≈ K/r); the **empty-pool isolation channel** e^(−(1−μ)(N−1)/B) — K-independent, RF cannot compensate — confirmed at B = 250 (57 + 61 bad/8 000; matched-degree ungated twin 0/8 000; naive transfer rejected ~430×) and B = 500 (0/500 good, strandings Poisson at E_iso = 5.3), the (1−μ) exponent confirmed across the μ axis; design rule **size the pool, not the headroom** — (N−1)/B ≥ ln(H/δ)/(1−μ) (B ≤ 185 at δ = 10⁻⁴), coverage-free at r ≳ 3; the ordered-predicate alternative priced in report §4 (measured by E19); N-039's three assumptions verified. The benefit-side flooding grid became E19 (the active branch).

- **experiments gate trade-off — E10 + E12** (merged 2026-08-12, PR #152; no Spec Kit). **E10 — selection-family fidelity**: eleven cells over the calibrated M2 bulk point (`configs/experiments/selection-fidelity/`, seeds 901–904/911–917; `docs/experiments/e10-selection-fidelity.md`) — gated picks reproduce the coverage law exactly at survivor headroom r = (N−1)/(B·K) ≥ 2 (pooled 279/32 000 vs the law's 0.0088), 5× cliff at r = 1, Family-A convergence below; gate-only doubles P(bad) at equal mean degree (the resurrected eclipsed-receiver class), and the **+1-link compensation** B = (N−1)/(RF+1) is measured (68/8000 vs predicted 0.0079). **E12 — flooding mitigation under the cap**: pilot + 48-cell B × cap × Sybil grid (`configs/experiments/flooding/`, seeds 1000–1048; `docs/experiments/e12-flooding-mitigation.md` + `summarise_flooding_cell.py`) — attacker concentration ≈ K/B wherever the cap leaves room; **starved honest links, not slot concentration, are the harm**; accounting identities 48/48. Joint deliverables: the **B trade-off table** (largest B with r ≥ 2 — 125 — coverage-exact and flood-resilient) and the **cap-sizing rule** (cap ≈ RF(1+2f) + c√RF, c ≥ 2 measured floor). **Machinery**: the per-node connection-accounting detail columns (`72bf76c`; run rows untouched, all seven baseline sweeps byte-identical ⇒ no re-baseline). Parked from the pass: the failure-severity d-distribution comparison (batches with any future row-schema change), partial-view sizing synthesis (`notes/view-size-synthesis.md`, exploration).

- **experiments model comparison** (merged 2026-08-06, PR #138, issue #86; the PR #119 follow-up — deliberately no Spec Kit; decision record **ADR 0041**; review record F1–F4 in `notes/experiments-model-comparison-review.md`). The per-model fidelity checks — all five dissemination models vs the formal closed laws + MC grids, in the m2-comparison mode (deterministic means exact; probabilities as counts + Wilson 95 % containing the published values). **Machinery complete (phases 0–4)**: (1) the **sends-by-kind split** — every dissemination send attributed at emission to its carrying link kind (relay wins the both-kinds dedup case), `sends_by_kind` on every publish slice, `sends_relay_mean`/`sends_publisher_mean` aggregates, and the second per-run identity relay + publisher = total sends; the feature's one row-schema change ⇒ the single re-baseline generation **`23d0223`** (all five sweeps value-identical to `7e50e3a`/`d7e7132` modulo the added fields; m2-comparison re-validated, "Schema extended" provenance row); (2) the **publisher-pair config surface** — per-class `publisher` sub-table (presence-activated; omitted from manifests when absent, so relay-only manifests are unchanged), `forward-to-all` legal, `publisher_pick_count` (k_out) axis that refuses to sweep an undeclared seam; (3) **`DisseminationModel` = m1–m5** — m3 keeps relay-only propagation edges with **seed-aware goodness** (per-publisher seed-closure walks on the condensation DAG; the formal M3 study's exact every-publisher check), m5/m1 extract the relay ∪ Active-publisher union, and **model↔wiring coherence** is validated per grid point on the honest class only (m2/m4 relay-only + forward-to-relays with m2 directional / m4 symmetric; m3 publisher table + forward-to-relays; m5/m1 publisher table + forward-to-all; m1 additionally relay pick 0 — free plane coordinates never constrained); (4) `m3-smoke`/`m5-smoke` shipped (suite pipeline health incl. both kind columns populated) and the living contract homes updated (`configs/experiments/README.md` = the schema + coherence rules; `docs/experiments-program.md` E6/E8 flipped to ready). Byte-identity spot-checks at each instrument-touching commit recorded in `notes/experiments-baselines/README.md`. Also carried: the PR #119 §1 housekeeping (LinkKey range-walk doc claim softened; config-error strings aligned with their load sites). **Program work executed**: E6/E7/E8 run and documented (`docs/experiments/{m3,m4,m5}-comparison.md`, M1 inside the m5 doc; cell configs shipped in `configs/experiments/comparisons/`, suite-validated) — 20 coverage-law cells at the formal validation rows (bulk → 30 000-run deep tail, both network sizes) all law-consistent, and the four P(bad) ≤ 10⁻⁴ operating points matching the published cost/latency/degree means at table precision (M3's seeding cost = the s−1 seeding sends, publisher-kind minus relay-wins overlaps; M5's kind split = the k_in : k_out ratio; M1's relay column ≡ 0). The M4 RF = 6 deep-tail cell completed the set (251/30 000 vs the law's 0.00836 — 1.00×; 21/21 cells aligned, nothing pending). Core (`NodeState`/`apply`/wire/handlers) untouched except cfg-gated experiment accessors.

- **017 — unified selection plane** (merged 2026-08-02, PR #119; spec dir `specs/017-unified-selection/`; ADRs **0039** (unified selection plane) / **0040** (selection randomness derivation); analysis ledger `analysis.md` I1–I6 plus the review round's docs rider and F1–F3 fixes; the post-implementation `/speckit-analyze` round closed at zero-finding convergence): one selection implementation per seam — `Selection` (`strategies/connection/selection.rs`) — over two fed knobs, **the bucket count** (hash-gate width, B ≥ 2 on the CLI, never derived: `resolve_buckets`/`bucket_count(len, target_degree)` deleted) and **the pick count** (exactly min(pick count, gate survivors) seeded uniform picks; the experiments `UniformSampler` promoted to a node capability and its type deleted), replacing the four dial strategies AND the four acceptance baselines — `UnifiedAcceptance` = gate verification following the seam's bucket count (`--relay-/--publisher-accept-unverified` opt-out) × an absolutely-fed accept cap (`accept_cap(K, c)`/`--cap-buffer` deleted; cap 0 = serve none via explicit `Rejected` — a deliberate change from the old silent drop). CLI is knob-only and presence-activated with zero boundary values for the `none` kinds (M1 = `--relay-pick-count 0`; publisher seam requires a dial knob); fan-out default flipped to `forward-to-all` (M3's `forward-to-relays` is the marked case — the footgun is stated in help + quickstart); `--selection-seed <u64>` (required iff sampling) expands at the loader (`SHA-256(lp("pubsub/selection-seed/v1") ‖ seed_le8)`) into the per-topic draw preimage `lp(domain) ‖ lp(seed) ‖ lp(self-id) ‖ nonce_le8 ‖ lp(topic)` under per-seam domains `pubsub/uniform-selection/{relay,publisher}/v1` (I2 closed the correlated-seam-draw defect). The (bucket absent, pick = RF) point + the ADR 0034 symmetric handshake = the **real M4**, label claimed (fleet evidence in `tests/model_family.rs`: reciprocity, min degree ≥ pick count, mean ≈ 2×). Experiments config speaks the same coordinates (`pick_count`/`bucket_count` axes, `symmetric` switch; boundary values legal as axis points); E7/E10/E12 read ready in `docs/experiments-program.md`. Validation contract discharged: commit A `aebd915` byte-identical against the recorded baselines; commit B landed the honest preimage with baseline generations **`7e50e3a`** (re-baseline) + **`d7e7132`** (first M4 baseline) recorded in `notes/experiments-baselines/README.md` and the m2-comparison re-validated (`docs/experiments/m2-comparison.md`). Notes: **N-036** (gate-failing dials = provable-but-unrecorded evidence), **N-037** (selection-seed privacy stand-in), **N-038** (sampled selection under view growth), N-032 trigger re-pointed. Core (`NodeState`/`apply`/wire/handlers) untouched.

- **016 — deterministic experiments framework** (merged 2026-07-23, PR #102; spec dir `specs/016-experiments-framework/`; ADRs **0035** (driver architecture) / **0036** (output contract + statistics conventions) / **0037** (serde_json); also carried the `docs/experiments-program.md` program of record — PR #74 closed as superseded). A feature-gated `experiments` module + `experiments` binary driving the pure core (`NodeState` + `apply`) under a round-based **wavefront scheduler** (content-keyed wave canonicalisation — determinism is driver-owned; core touched only via crate-internal cfg-gated access, e.g. `Message::connection_parts()`). Vocabulary: **run** (one seed: registration → dial drain → churn draw → SCC passes → publish drain → measure) / **experiment** (R runs, one parameter set) / **sweep** (experiments serving one question); runs are pure functions parallelised at sweep-wide run granularity (`std::thread::scope` pool; byte-identical artifacts at any worker count). Two measurement instruments: the **publish drain** (coverage with excluded-publisher denominator, per-node first-receipt depth, miss-cause decomposition, sends split by recipient class + suppressed + the per-run accounting identity) and **realised-graph analytics** (degree/sink stats; **good-topology ⟺ one SCC** of the up-honest propagation digraph, post-churn primary + pre-churn diagnostic, min-publisher-coverage from the condensation's sink components; parameterised as (digraph, per-publisher seed set) behind a `DisseminationModel` dispatch that owns extraction — v1 ships **M2 only**, reading the kind-filtered `downstream_relays()` accessor: publisher links are seed edges, never propagation edges). **Honest churn** is a first-class seeded parameter (down ≠ unregistered; no events; adversary count unchanged). Probabilities always as **raw counts + Wilson 95%** (the formal folder's ±1σ degenerates at all-good samples — methodology note recorded in the documented comparison). Output contract: **three artifacts** (manifest / run-records JSONL / aggregates), aggregates a pure function of the rows, nothing O(N) by default, per-node detail opt-in, no resume (re-run; canonical-order prefix on interruption). Ships two **experiments-only strategies** (`SilentRelay` fan-out; `UniformSampler` dial — exactly-RF uniform picks, the formal M2 selection family, min(target_degree, available) degeneracy) and the **M2-comparison demonstration** in [`docs/experiments/m2-comparison.md`](docs/experiments/m2-comparison.md) (operating point N=20 000/μ=0.2/RF=24, master seed 42 + the bulk-regime P(good) point = the formal tail ladder's N=4 000/μ=0.2/RF=16 cell, master seed 4016; cost/latency and P(good) agree with the formal values within sampling noise — depth means exact, bulk P(bad) 76/8000 with the law's 0.0088 inside the Wilson interval; **re-validated byte-identical** across the 015 integration at tool commit `493bb2f`; suite smoke variant asserts pipeline health only). Honest fan-out = **`forward-to-relays`** (the crate's default policy; v1 configs reject `forward-to-all` — the M5 all-links kind waits for publisher-link experiment support); μ in the shipped configs maps to `adversarial_fraction` (m2 defines μ = k/N, silent relays; no separate churn term); population memory is O(N²) — ~30 GB at N = 20 000, the worker count doubles as the memory knob (N-033). One new dependency: `serde_json`, optional, feature-tied. Artifacts: spec (Input verbatim + 2 clarify sessions), plan + plan-input.md (verbatim), research (R1–R12), data-model, contracts/{output-artifacts,sweep-config}.md, quickstart, checklists/requirements.md; the post-implementation `/speckit-analyze` round closed at zero-finding convergence.

- **015 — publisher links + M3/M4/M5 model configuration** (merged 2026-07-23, PR #77, issue #87; spec dir `specs/015-publisher-links/`; full exploration preserved on `archive/015-full-exploration`; ADRs **0032 + 0034**, plus **0033** = config bootstrap-`peers` removal via sub-PR #98, closing N-007). **State**: the two collections `NodeState.upstream`/`downstream` are `BTreeMap<LinkKey, LinkState>` with `LinkKey { topic, peer, kind }` (topic-first Ord) over `LinkKind { Relay, Publisher }` (the stored **traffic class**) — direction is *which field*; dialed entries (relay up, publisher down) carry the `AwaitingAccept→Active` lifecycle, accepted entries insert `Active`. **Wire/vocabulary** (ADR 0034, review round 5): one connection `Message` variant per **handshake** — `RelayConnection`/`PublisherConnection`/`SymmetricConnection` over `PlainConnection { emitter, action }`; `signed_bytes(HandshakeKind)` binds the tag (0x00/0x01/0x02) from the variant; `apply` routes each variant to `state/handlers/{relay,publisher,symmetric}.rs` (shared mechanics = composed helpers in `handlers/mod.rs`). **Symmetric (M4-approx)**: reciprocity is **constructed** — one accept records the relay-class link in BOTH maps on both ends, dialer mirrors on Accepted, Terminated/severance remove both halves; `NodeStrategies.symmetric_edges` switches vocabulary + mirroring (non-symmetric nodes drop inbound symmetric handshakes and vice versa); capped acceptance composes (whole-edge refusal); the **M4 label is deferred** until a uniform exactly-RF selection kind lands (follow-up, with B-as-parameter; N-032 = what a cap bounds on a symmetric node, deferred). **Seams reused**: the publisher pair is `Option<Arc<dyn …>>` second instances on `NodeStrategies` (`relay_only()` = M2 shape); `ConnectionStrategy::expected_links` (renamed from `expected_upstream`); hash-gated strategies carry `for_kind` (domain: `relay-edge/v1` — renamed from `edge/v1` — vs `publisher-edge/v1`) and `with_symmetric` (`edge-sym/v1`, canonical-pair); acceptance capacities are disjoint per kind. **Fan-out**: `targets(topic, downstream, origin, exclude)` per-peer deduped — `ForwardToRelays` (default: forward to relay downstream, seed own publications over publisher-Active = M2/M3) | `ForwardToAll` (every held message over both kinds = M5, `--fanout-strategy`). **Gate**: kind-agnostic — any `Active` upstream entry for `(topic, from)` admits, either kind (A13: a receive-side restriction exists only if checkable from the signed bytes alone; owner-binding removed, `PublisherAdmission` + `--publisher-admission` deleted — M3's exclusivity is the sender-side fan-out; M3 vs M5 differ ONLY in `--fanout-strategy`); severance removes the **admitting** link (both halves on a symmetric node); frame-sender authentication recorded as a transport obligation (N-013). **Getters**: `upstream_relays()`/`downstream_relays()` (renames) + `upstream_publishers()`/`downstream_publishers()`. **CLI renames**: `--relay-strategy`/`--relay-acceptance-strategy`/`--relay-degree` (+ publisher mirrors); `--genesis`/`--bucket-count`/`--cap-buffer` shared across seams. Model recipes in `specs/015-publisher-links/quickstart.md`; analysis ledger (A1–A13) in `analysis.md`.

- **005 — peer-view** (merged 2026-07-08, PR #73, issue #75). Realises the **verifiable hash-gated (bucketed-pull)** overlay mechanics of [`../docs/extensions/bucketed-pull.md`](../docs/extensions/bucketed-pull.md); full suite + clippy + fmt green. **Module layout** (ADR 0029): all strategy policy under `strategies/` (`connection/`, `acceptance/`, `fanout/` — trait in `mod.rs`, one file per strategy — plus `config` = ADR 0028 two-phase construction, **`edge`** = the shared verifiable predicate, and `test_support` = shared strategy-test builders); connection **lifecycle state** (`UpstreamState`, `test_support`) is core domain state in `connection_state`. Strategies stay **injected at `Node::new`** (pure; target_degree/cap-buffer/bucket-override are fields; the epoch nonce is a view input). **Epoch/Round split** (ADR 0031): **`Event::Heartbeat`** (parameterless dial tick, the "round" — repeated heartbeats within an epoch re-dial the SAME expected set, the retry primitive) vs **`Event::Epoch { nonce: u64 }`** (folds `NodeState.epoch_nonce`, the randomness context; fold emits no effects — re-dialing is the driver's choice via a following Heartbeat). `--genesis` = the **initial** epoch nonce, threaded through `Node::new` into state; **v1 fires one heartbeat at readiness and never fires Epoch**. **Shared edge predicate** (`strategies::edge`, ADR 0024/0031): `is_valid_edge(nonce, topic, requester, candidate, buckets) = SHA-256(…) mod buckets == 0` (directional, cross-machine stable; raw key bytes via the crate's one length-prefix primitive; `EDGE_DOMAIN …/edge/v1` — pre-release iterations keep v1), `bucket_count(len, target_degree) = max(1, round(len/target_degree))` (integer math), `resolve_buckets(override, len, target_degree)` = the ONE derive-or-override site, `accept_cap(target_degree, c) = ⌈target_degree + c·√target_degree⌉`. **Dial**: `HashGatedConnection { self_id, target_degree, bucket_override }` — dial candidate iff the edge predicate holds; expected out-degree ≈ **fixed `target_degree`**; small topic → `B=1` → connect-to-all (note: a pinned `--bucket-count` bypasses that floor). **Accept — four one-dimensional baselines** (ADR 0031, empirical approach): `AcceptFromAllCandidates` (membership only), `BoundedAcceptance` (cap only), `HashGatedAcceptance` (gate only), `HashGatedBoundedAcceptance` (gate ∧ cap — the compound, ex-`VerifiableBounded`); the three refusing policies run the shared `admit_prelude` first (membership → idempotent already-downstream re-Accept, one borrow-only downstream scan — the half-open-link repair lives once). `Admission { Accept, RejectMembership, RejectIllegitimate, RejectOverCapacity }`; **silent drop** for membership/predicate failure, explicit **`ConnectionAction::Rejected`** only for over-capacity → dialer removes the pending `AwaitingAccept`, **no retry/back-fill**. **Readiness gate** (ADR 0031): inbound `Request`s before `Synced` are dropped (`not_synced`) — closes the fail-open pre-snapshot window where a partially-folded candidate view floors B to 1; post-sync fold lag remains the documented B-agreement assumption (`--bucket-count` = the by-construction fix). The read-only **`NodeView`** (`subscriptions`/`candidates`/`downstream`/`epoch_nonce`) is what the seams take — `expected_upstream(&NodeView)` / `admit(emitter, topic, &NodeView)`. **Config**: `--genesis` (initial epoch nonce, default 0), `--target-degree` (required by all but connect-to-all/accept-from-all), `--cap-buffer` (`c`, default 3; bounded/hash-gated-bounded), `--bucket-count` (optional pinned B, both seams). CLI kinds: `connect-to-all`/`hash-gated`; `accept-from-all`/`bounded`/`hash-gated`/`hash-gated-bounded` — mixed pairs are legal experiment configs. **Two-phase construction** (ADR 0028): per-seam params (`{self_id, target_degree, bucket_count}` / `{self_id, target_degree, bucket_count, cap_buffer}`), shared validators `require_target_degree`/`validate_bucket_count`, one aggregate build + one error map. **Fan-out unchanged** (`ForwardToAll`). ADRs **0024** (verifiable hash-gated selection), **0025** (acceptance seam + `Rejected`), **0028** (two-phase construction), **0029** (`strategies/` module grouping), **0030** (heartbeat + shared edge predicate), **0031** (epoch/round split + readiness gate + decomposed acceptance + naming). **Out of scope**: the **incentive/chain layer** of bucketed-pull (deposits `D`, sybil bound `K`, on-chain identity, over-capacity slashing reports), the real unbiasable beacon (the epoch nonce is its seam; genesis stands in for `nonce_R`), discovery/view sampling (`H_v`; v1 view = full candidate set), periodic heartbeats + epoch advancement + rotation/teardown, golden/relay tiers, the experiment/testing framework.

- **004 — node event-loop refactor** (PR #50). The pure core: crate-internal `NodeState` + pure `apply` → `Vec<Effect>` (`Effect` uninhabited), named per-variant handlers (`apply` → `handle_message_received` → `handle_signed_message`), `Arc<Mutex<NodeState>>` shell with sync getters + the single event queue (`Event`/`EventQueue`) drained by one loop with node-owned producers via `spawn_producer` (`network_mailbox_loop`), spawn-in-constructor + drop-abort. ADR 0011/0012. (Connection model deferred to a follow-on `004-connections`.)
- **008 — subscription registry** (PR #51). The in-memory **subscription list** (node membership; distinct from the topic registry — no shared trait): `SubscriptionRegistry` (single node-keyed `watch(node)`) + `SubscriptionRegistryControl` (`set_topics`/`unregister`) + `InMemorySubscriptionRegistry` (`new()`/`from_file`); `MembershipEvent` (Joined/TopicsChanged/Left) / `MembershipWatch` (push, mirrors `NetworkHandle`/ADR 0007). `Event::MembershipUpdate` + `handle_membership_update` fold the node's **own** entry → `subscriptions` and **other** nodes → per-topic `candidates` (self-excluded, distinct from config bootstrap `peers`; N-007); `Node::candidates` getter. The subscription list — not config — is the source of truth for a node's own topics (ADR 0013/0014): `Node::new` takes the registry generically as `Arc<R>`, no `initial_subscriptions`, no fail-fast on a missing entry; the node is read-only and the local `subscribe`/`unsubscribe` mutators are removed (ADR 0015); 002's `subscribed_topics` config field is removed.
- **013 — topic registry** (merged 2026-06-12). The in-memory **topic registry**: which topics legitimately exist + each topic's authorized publisher keys (empty set = open topic), the topic-governance counterpart to 008's subscription list (no shared trait). `TopicRegistry` (single **global** `watch()`) + `TopicRegistryControl` (`set_topic`/`remove_topic`) + `InMemoryTopicRegistry` (`new()`/`from_file`); `TopicRegistryEvent` (Registered/PublishersChanged/Removed) / `TopicRegistryWatch`. `Event::TopicRegistryUpdate` + `handle_topic_registry_update` fold `registered_topics: HashMap<TopicId, BTreeSet<PublicKey>>` into `NodeState`; effective accept-filter = `subscriptions ∩ registered_topics` (single `Node::subscriptions()` getter returns it); `handle_signed_message` drops `topic_not_registered` and `publisher_not_authorized` before signature verification. `Node::new` gains a third generic registry param (named `subscription_registry`/`topic_registry`); `PublicKey` gains `Ord`. **ADR 0016** (topic-registry interface + node integration). Publisher keys ≡ subscription-list node id at 011 (N-009).

- **004-connections — logical connections** (merged 2026-06-16, PR #56). Per-`(peer, topic)` connections on `NodeState`: `upstream` (`AwaitingAccept`/`Active`) + `downstream`; strategy-driven establishment (`ConnectionStrategy`/`ConnectToAllCandidates`) on a setup event; signed control messages (`Message::Connection`, emitter inside the signed bytes); membership-validated idempotent acceptance; connection-gated receive path (connection → subscription → registered → authorized → signature) with silent signature-only severance (`Effect::Misbehaved`); consuming `Node::shutdown(self)`; **`PeerId` wraps `PublicKey`**; shared signing-bytes helper. ADRs **0017/0018/0019**. (014 then rebased onto this: extends the cascade to `upstream`/`downstream`, replaces the `connection_setup_delay` timer with an `Event::Synced` readiness transition pushed by the single registry indexer once both registry snapshots are folded, and resolves S7/N-015.)
- **014 — registry consistency** (merged 2026-06-18, PR #63). Elevates 013's read-time `subscriptions ∩ registered_topics` into a **maintained `NodeState` invariant** (`subscriptions`/`candidates ⊆ registered_topics.keys()`) via **strict-drop** folds (membership for an unregistered topic dropped + logged; no declared/pending buffer; no auto-promotion — **013 SC-004 removed**); **defensive registry fold** (create-only-on-`Registered`, no `or_default`); **atomic cascade** on `Removed` (clears subscriptions + candidates + projection + `upstream`/`downstream` in one fold); declarative **`TopicEntry`** (`pub(crate)`; `is_open`/`is_publisher_authorized`; `registered_topics: HashMap<TopicId, TopicEntry>`). **Readiness/dial**: each `watch()` returns a `(snapshot, live-watch)` pair (`TopicSnapshot`/`MembershipSnapshot`, no `SnapshotComplete` variants); a single `registry_indexer_loop` folds the topic snapshot before the membership snapshot, then pushes `Event::Synced` (readiness → `Syncing`/`Synced`, `Node::is_synced()`) which runs `handle_connection_setup` — **replacing the `connection_setup_delay` timer**; `Event::ConnectionSetup` is retained as the dial action (tests/operator/epochal). Resolves **S7/N-015**. **ADR 0020** (amends 0016).

- **006 — fan-out policy** (merged 2026-06-19, PR #67). Publish + fan-out on the 004 topology: `Node::publish(SignedMessage)` → `Event::Publish` → `handle_publish`; verbatim fan-out via an injected `FanoutStrategy` seam (v1 `ForwardToAll`) with receive-path split-horizon; content-hash dedup (`seen`, after signature verification, cause `duplicate`); `ReceivedDelivery` carries `origin: Origin { Local, Peer(PeerId) }`; subscriber-relay. ADR **0021**.

The node event-queue boundary shared across the registry features is specified in **`specs/event-loop-and-registry-contract.md`**.

**Most recently completed feature**: **003-message-envelope-mock-crypto** — the signed message envelope + mock crypto. Canonical reference under `specs/003-message-envelope-mock-crypto/` (plan, research, data-model, contracts, quickstart). Key decisions:

- ADR 0009 `docs/decisions/0009-crypto-trait-shape.md` — crypto trait shape (concrete byte newtypes, no associated types, mock-crypto factory shape).
- ADR 0010 `docs/decisions/0010-protocol-message-type-hierarchy.md` — `Message` enum + `SignedMessage` / `PlainMessage` split + 001 `Envelope` → `RoutingFrame` rename + `MessageHash::of(&PlainMessage)` content-anchored hash.
- Introduced the `Signer` / `Verifier` traits + `crypto::mock`, the `Arc<dyn Verifier>` `Node::new` parameter, and the `message_dropped` / `cause` drop-event convention (which replaced 002's `topic_drop`). Everything 002 contributed (the `TopicId` newtype, the subscription set + `subscribe`/`unsubscribe` API, the `subscriptions()` snapshot, the TOML `subscribed_topics` field) propagates unchanged.

**Workstream-level docs (sibling to feature dirs)**:
- `specs/event-loop-and-registry-contract.md` — shared contract for the two active features (004 / 008); both their specs cite it.
- `specs/IMPLEMENTATION_NOTES.md` — deferred implementation questions to revisit (currently N-001 for local-emission/local-receipt under a future REST API; N-002 for self-addressing under connection-based transports in feature 004+; N-003 for chain-integrity / equivocation / publisher-authorization validation under the registry features (**publisher-authorization slice closed by 013**, ADR 0016; equivocation / parent-hash / sequence / deposit remain at 012); N-004 for CBOR-canonical encoding swap under feature 009 or first cross-language consumer; N-005 for the `MessageHash::of(&PlainMessage)` content-anchored hash decision, to revisit when downstream features first operationally consume the hash — chain-integrity validation at 008 / 012, future caching / dedup; N-006 for the construction-failure (duplicate-registration) integration test, deferred from 004's parity scope to 004-connections; N-007 for `peers` placement — shell field today; **resolved for 008**: the `MembershipUpdate` arm folds a topic-derived candidate set into `NodeState`, kept distinct from the config bootstrap `peers` shell field (ADR 0014); the static `peers` field stays on the shell for the future dialer; revisit again at 005 (`PeerView`)).
<!-- SPECKIT END -->

# pubsub-node — agent guidance

Rust implementation of the Cardano PubSub node. Project-level context is in the parent `pubsub/CLAUDE.md`; this file covers what an agent working inside `pubsub-node/` needs to know.

## Authoritative documents

- **Constitution**: `.specify/memory/constitution.md` (v1.2.0) — five principles (I–V) plus Engineering Standards and Development Workflow rules; honour all of them before authoring code or specs. (Notably: logs are operator UX, not a test surface; operator-facing strings carry no FR/spec citations; parse at the edge; forward-compatible interfaces are justified by a ROADMAP consumer; multi-step test state is built via declarative test-only helpers; `/speckit-analyze` findings are recorded in the feature's `analysis.md`.)
- **Feature specs** (agent-editable): `specs/NNN-<short-name>/spec.md`, produced by `/speckit-specify`.
- **ADRs** (agent-authored): `docs/decisions/NNNN-<title>.md` — structural decisions, per Constitution Principle III.
- **Protocol specifications (READ-ONLY per Principle V)**: `../formal_spec/`, `../docs/`, `../docs/extensions/`. Surface ambiguity via ADR or issue; do not edit.

## Spec Kit workflow

Features flow through: `/speckit-specify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement`. Small fixes may bypass.

### Manual branch step — do this BEFORE `/speckit-specify`

`.specify/` lives inside the parent `pubsub/` git repo, not at the git root. Because of this subfolder layout, the Spec Kit git extension does **not** auto-create the feature branch — `/speckit-specify` emits:

    [specify] Warning: Git repository not detected; skipped branch creation

This is expected, not an error: Spec Kit tracks the feature by directory name (`specs/NNN-<short-name>/`); the git branch is **your** responsibility, and you MUST cut it yourself **before** running `/speckit-specify` so the feature work is isolated from `main`.

Procedure:

1. **Confirm you are on `main`** — `git branch --show-current` MUST print `main`. If you are already on a feature branch, do NOT branch again; go straight to `/speckit-specify`.
2. **Cut the branch from `main`**, named `NNN-<short-name>`:

       git checkout -b NNN-<short-name>     # e.g. 008-topic-registry

3. Then run `/speckit-specify <description>`.

Make the branch's short-name match what Spec Kit derives from your description so the branch and the `specs/NNN-<short-name>/` directory line up. If they diverge it's harmless — the directory name is the canonical feature identifier; the branch is for git/PR workflow only.

**Next-up branches** (per the two active features above):

- `004-node-event-loop` — the node event-loop refactor + connection model (project author).
- `008-topic-registry` — the mock topic registry (co-developing architect).

Per the parallel-work plan in `specs/event-loop-and-registry-contract.md`, the refactor (004) lands to `main` first; 008 then branches from the updated `main` so `Event` / `EventQueue` / `spawn_producer` already exist. If both must start from today's `main` at once, see the "seam commit" note in that doc.

## Commit discipline (Constitution §3)

- Every commit MUST compile and pass all non-ignored tests.
- Commits MUST be logical increments leaving the repo at a functional checkpoint.
- Skipped/ignored tests MUST carry a reason and a tracking issue or ADR reference.
