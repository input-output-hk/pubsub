# Specification Quality Checklist: Topics + Topic-Subscription Filtering

**Purpose**: Validate specification completeness and quality before proceeding to planning
**Created**: 2026-05-29
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] No implementation details (languages, frameworks, APIs) — spec describes behavior and API contract shape; type names (`TopicId`, `SubscribeOutcome`) are spec-level identifiers carried over from chat-locked decisions, not framework choices
- [x] Focused on user value and business needs — user stories framed around the developer/operator integrating the Node
- [x] Written for non-technical stakeholders — pubsub-node's "stakeholders" are technical (developers, SPOs); spec matches 001's level of detail
- [x] All mandatory sections completed — User Scenarios, Requirements, Success Criteria, Assumptions all populated

## Requirement Completeness

- [x] No [NEEDS CLARIFICATION] markers remain — all decisions resolved during pre-spec chat
- [x] Requirements are testable and unambiguous — each FR specifies an observable behavior
- [x] Success criteria are measurable — SC-001 through SC-007 give concrete pass/fail conditions
- [x] Success criteria are technology-agnostic — describe observable behavior, not implementation
- [x] All acceptance scenarios are defined — each user story has Given/When/Then scenarios
- [x] Edge cases are identified — empty subscriptions, idempotent re-calls, duplicate TOML entries, local-emission-not-receipt, inherited 001 cases
- [x] Scope is clearly bounded — explicit out-of-scope list at the bottom of the input section and in Assumptions (no wildcard, no persistence, no registry, no crypto, no connection-close)
- [x] Dependencies and assumptions identified — inherits 001 assumptions explicitly; forward links to 003 (crypto), 004+ (connection-close), 008 (registry)

## Feature Readiness

- [x] All functional requirements have clear acceptance criteria — FR-004 mapped to US1/US2/US3 scenarios; FR-006 mapped to US3 + SC-005; FR-010 mapped to US4; FR-011 mapped to SC-006
- [x] User scenarios cover primary flows — single-topic filter (P1), multi-topic N-node (P2), dynamic transitions (P3), TOML loading (P4)
- [x] Feature meets measurable outcomes defined in Success Criteria — each SC traceable to a user story or FR
- [x] No implementation details leak into specification — no runtime choice, no module structure, no concrete data structure beyond the API surface

## Notes

- Items marked incomplete require spec updates before `/speckit-clarify` or `/speckit-plan`
- All checklist items pass on the initial validation pass (no iterations required)
- Dynamic-subscription user story (US3) was added in response to user feedback during initial drafting — the originally-drafted spec covered only static subscriptions in P1/P2 and TOML loading in P3, leaving the runtime mutation API surface unexercised at the integration level

---

# Pre-`/speckit-tasks` Readiness Pass

**Purpose**: validate requirements quality across spec.md + plan.md + research.md + data-model.md + contracts/ as a whole, in preparation for `/speckit-tasks`.

**Audience**: reviewer / co-maintainer (also usable as author self-check).

**Created**: 2026-05-30 (first pass).

Numbering continues from the 16 items above (last legacy item is the implicit CHK016 of the original spec-quality pass).

## Naming & Terminology

- [x] CHK017 - The TOML config file is named `peer-list.toml` and the parsed type is `PeerListConfig`, but the v2 schema now carries `subscribed_topics` in addition to `[[peers]]`. Is a broader name (e.g., `node-config.toml` / `NodeConfig`) considered and justified, or is the legacy name preserved with explicit rationale for the carrying-cost? [Naming, Consistency — Spec §FR-010, contracts/peer-list.toml.md, data-model.md §5] — _resolved 2026-05-30: renamed in 002. `PeerListConfig` → `NodeConfig`; `load_peer_list` → `load_node_config`; `RawPeerListConfig` → `RawNodeConfig`; contract file `contracts/peer-list.toml.md` → `contracts/node-config.toml.md` (via git mv to preserve history); contract file title "Peer-list TOML schema" → "Node-config TOML schema"; the constructor parameter `peer_list: PeerListConfig` → `config: NodeConfig`; quickstart fixture suggestions `node-a.peers.toml` → `node-a.toml`. 001 artifacts are unchanged — the legacy "peer-list" name persists in 001's `contracts/peer-list.toml.md` as historical record. Every 002 artifact (spec / plan / research / data-model / contracts / quickstart) carries a one-line migration note pointing back to CHK017 so future readers see the rename intent without needing to grep this checklist. The CHK033 cross-reference was updated to the new file path._
- [x] CHK018 - Is the canonical phrase "topic-based subscription filtering" used consistently across spec, plan, research, data-model, contracts, and quickstart? Or does any artifact drift into "topic-based routing" / "subscription routing" / "topic routing"? [Terminology Consistency] — _verified 2026-05-30: the only mentions of "routing" in 002 artifacts are contrastive disclaimers (spec L19 and L186 explicitly rule it out: "not topic-based routing — the network does not route by topic, the receiver filters by topic"). No drift._
- [x] CHK019 - The rename "Message enum → Message struct + MessagePayload enum" is explicit in research.md §1 and contracts/library-api.md, but the spec text stays at "every Message carries a topic field" without naming MessagePayload. Is this layering deliberate (spec at API-surface level, plan at internal-shape level) or is a normative spec sentence needed? [Ambiguity, research.md §1 vs Spec §FR-001] — _resolved 2026-05-30: intentional layering — spec stays agnostic to whether topic is a struct field or embedded in variants (both shapes satisfy FR-001's "every Message carries a topic"); plan / research / contracts pin the struct shape as 002's concrete choice. Same pattern as CHK023 (subscriptions() return type). Construction-shape ergonomics for callers are documented in plan / data-model / quickstart, so discoverability of the rename is preserved._
- [x] CHK020 - Are both `SubscribeOutcome` and `UnsubscribeOutcome` variant names (Added / AlreadyPresent / Removed / NotSubscribed) used identically across spec FR-006, US3 acceptance scenarios, data-model.md §4, contracts/library-api.md, and the tracing-event field shapes in research.md §7? [Consistency] — _verified 2026-05-30: variant names identical across all five locations; no drift._

## Cross-artifact consistency

- [x] CHK021 - Does the Node constructor's new 4-parameter signature (`self_id, peer_list, initial_subscriptions, network`) appear identically across plan.md, data-model.md §3, and contracts/library-api.md? Same parameter order, same types (especially the `HashSet<TopicId>` typing of `initial_subscriptions`)? [Consistency, plan.md vs data-model.md §3 vs contracts/library-api.md] — _verified 2026-05-30: 4-parameter signature consistent. Parameter name `peer_list` renamed to `config` per CHK017; type renamed `PeerListConfig` → `NodeConfig`. `initial_subscriptions` typed as `HashSet<TopicId>` everywhere. Order `self_id, config, initial_subscriptions, network` matches across all three artifacts._
- [x] CHK022 - Are the TopicId validation rules (non-empty, no NUL, no extra character-class restrictions) stated identically across spec FR-002, data-model.md §1, contracts/library-api.md "TopicId" section, and Clarifications Q2 round 1 (2026-05-29)? [Consistency, Spec §FR-002] — _verified 2026-05-30: rules stated identically across all four locations. research.md §6 references back to Clarifications Q2 for the rationale._
- [x] CHK023 - Does the `subscriptions()` return type wording match across spec FR-013 ("`Vec<TopicId>` or equivalent owned collection"), research.md §4 (concrete `Vec<TopicId>`), data-model.md §3 (concrete `Vec<TopicId>`), and contracts/library-api.md (concrete `Vec<TopicId>`)? Is the "or equivalent" in the spec a deliberate flexibility for future iterations or stale wording the plan locked down? [Clarity, Spec §FR-013] — _resolved 2026-05-30: deliberate layering — spec keeps the contract-level shape ("owned snapshot collection") so a future iteration may wrap the result in an opaque `Subscriptions` type without amending FR-013; the 002 concrete choice (`Vec<TopicId>`) is pinned in research.md §4, data-model.md §3, contracts/library-api.md. FR-013 grew a one-line cross-reference making the layering self-documenting; research.md §9 already records the forward-flexibility intent (the `Subscriptions` opaque type open follow-up)._
- [x] CHK024 - Are the five tracing event markers (`topic_drop`, `topic_subscribed`, `topic_unsubscribed`, `topic_subscribe_noop`, `topic_unsubscribe_noop`) and their structured fields (`event`, `self_id`, `from`/`topic`, `reason`) defined identically across research.md §7, contracts/library-api.md tracing-events table, and quickstart.md §7? [Consistency, FR-011 + FR-014] — _verified 2026-05-30: events and field shapes consistent across all three artifacts. After CHK025 a sixth event was added (`topic_config_duplicate`, warn level, target `pubsub_node::config`); the three artifacts were swept together so the addition is consistent. contracts/library-api.md grew a `Target` column for clarity._
- [x] CHK025 - `subscribed_topics` deduplication: spec's Edge Cases bullet says "The TOML loader MAY issue an operator-facing warning"; contracts/peer-list.toml.md says "the loader does NOT warn on duplicates in v2"; data-model.md §5 says deduplication happens "by the loader or by `HashSet` construction at Node construction time; either is acceptable". Are these three statements internally consistent — or do "MAY warn" vs "does NOT warn" contradict each other? [Conflict, Spec §Edge Cases vs contracts/peer-list.toml.md vs data-model.md §5] — _resolved 2026-05-30: extended FR-010 to mandate a warn-level structured tracing event on duplicate `subscribed_topics` entries (one event per duplicated topic per load call), emitted from `config::load_peer_list` with fields `event=topic_config_duplicate`, `topic`, `config_path` (no `self_id` — loader runs before any Node exists). Spec Edge Cases bullet updated from "MAY" to "MUST per FR-010". Contracts/peer-list.toml.md duplicate-row in field table rewritten to describe the warn; new example added. data-model.md §5 loader pipeline step 4 extended with the dedup-scan sub-step. research.md §7 tracing-events block grew a sixth entry covering the warn (target `pubsub_node::config`, asymmetry recorded). contracts/library-api.md tracing-events table grew a row + Target column added for clarity + field-formats text updated. quickstart.md §5 grew a duplicate-topic example; §7 event-table grew a row (renamed "four new structured events" → "six new structured events"); §6 grew a new test name. "v2" terminology cleaned up across contracts/peer-list.toml.md (section header and forward-compat note now reference "002")._
- [x] CHK026 - Test file names (`topic_filter.rs`, `topic_runtime.rs`, extensions to `two_node_ping.rs` / `n_node_graph.rs` / `config_loading.rs`) appear across plan.md, data-model.md §9, contracts/library-api.md, quickstart.md §§2–6. Are all five files referenced identically (same name, same user-story mapping) in every artifact that mentions them? [Consistency] — _verified 2026-05-30: file names and US mappings consistent across plan.md project-structure tree, data-model.md §9 test-fixture-impact section, and quickstart.md §§2-6. contracts/library-api.md does not enumerate test files (out of contract scope, which is correct)._

## Traceability

- [x] CHK027 - data-model.md §7 provides an FR → entity/file matrix. Is there a parallel matrix or table somewhere that traces each FR to at least one user-story acceptance scenario (or explicit non-trace, like FR-005's "enforced by empty file diff")? [Traceability, Gap] — _resolved 2026-05-30: added data-model.md §7.5 "Cross-reference matrix (FR → user story / acceptance scenario)" — a complement to §7. The matrix surfaces every FR + SC and lists either the test-anchored AS coverage or an explicit "not test-anchored (operator UX / forward-flexibility)" note. Two gaps the matrix surfaced were fixed in the same pass: (a) FR-010's duplicate-warn case had no AS coverage — added US4 AS-6 anchoring the *testable* behavior (deduplicated `subscriptions()` snapshot); log emission stays operator UX, not test-asserted, per the user's "logs are not something to test" framing. (b) FR-014's mutation logs and FR-015's concurrent linearization are documented in the matrix as deliberately not test-anchored at v1 — logs are observability, concurrent linearization is forward-flexibility under the single-process Trust assumption. quickstart.md §6 test name updated: `duplicate_subscribed_topic_emits_warn_event` → `duplicate_subscribed_topic_yields_dedup_set` (the test asserts on the snapshot, not the log)._
- [x] CHK028 - Does the linearizability contract (FR-015) have a corresponding acceptance scenario or success criterion that exercises it? US3 covers single-task transitions; SC-005 covers idempotency; neither asserts FR-015's *concurrent* linearization claim. Is concurrent linearization intentionally unexercised at v1 (single-process Trust assumption) or a coverage gap? [Coverage, Spec §FR-015 vs US3 / SC-005] — _resolved 2026-05-30: intentional non-coverage at v1. FR-015 is a forward-flexibility contract (mostly normative for future iterations introducing persistence / distribution); the natural `Arc<Mutex<HashSet>>` impl satisfies linearizability trivially without exercising it. Writing a concurrent-mutation AS would couple tests to tokio scheduler behavior with no real bug-catching power at v1. Rationale already recorded in data-model.md §7.5 ("not test-anchored at v1 — forward-flexibility contract")._
- [x] CHK029 - Does every research.md §"Open follow-ups" item have a traceable trigger condition (e.g., "when feature 008 lands") that maps to an existing ROADMAP entry, an IMPLEMENTATION_NOTES.md note, or a workstream document? Items without a concrete trigger risk being silently rediscovered. [Traceability, research.md §9] — _verified 2026-05-30: feature-anchored items (008 registry, 004 connection-model, 003 envelope) match ROADMAP §2 entries. N-001 / N-002 cross-references match IMPLEMENTATION_NOTES.md. Forward-flexibility items without a specific feature number (persistence, opaque `Subscriptions` type) have operator/demand-driven triggers — appropriate non-anchoring for items that wait on use-case emergence rather than a known feature._

## Edge Case Coverage

- [x] CHK030 - The self-addressing Edge Case bullet (added 2026-05-29) cites "FR-005 + FR-009 carve-out" as the deterministic answer. Is FR-009's "deliveries arriving through the receive path are valid receipts" wording precise enough that an implementer reading FR-009 alone (without the Edge Case bullet) derives the same answer, or does the answer depend on the Edge Case gloss? [Clarity, Spec §FR-009 vs Edge Cases] — _verified 2026-05-30: FR-009 says "the snapshot is strictly inbound-from-the-network: only deliveries arriving through the receive path are recorded". A self-addressed message travels through the InMemoryNetwork's loopback (per `src/network.rs:50-72`, no `from == to` short-circuit) and arrives at the receive path; the FR's text by direct reading recognizes it as a recorded delivery. The Edge Case bullet is operator-facing convenience, not a normative supplement. No FR edit needed._
- [x] CHK031 - Is there an edge case bullet for "two concurrent `subscribe(T)` calls from different tasks on the same Node" — which FR-015 promises is linearizable but which no US scenario exercises? Either the coverage is missing or the constraint is intentionally untested at v1. Should that intent be recorded? [Coverage, Spec §FR-015 vs Edge Cases] — _resolved 2026-05-30: intentional non-coverage (same answer as CHK028). data-model.md §7.5 records FR-015 as "not test-anchored at v1 — forward-flexibility contract". Adding an Edge Case bullet that re-states "concurrent mutation is linearizable per FR-015" would duplicate the FR text without adding anything._
- [x] CHK032 - Is there an edge case covering "Node restart with `subscribed_topics` in TOML, after the operator removed entries via `unsubscribe` in a previous run" — i.e., does restart reload the original TOML set or remain at the in-memory final state? The "no persistence" assumption answers this implicitly; is it spelled out? [Gap, Coverage] — _verified 2026-05-30: spec Assumptions section explicitly states "Restarting a Node re-reads the TOML; subscribe / unsubscribe calls made during a previous run do not survive. Persistence is out of scope for this feature." That's the normative answer. No edge-case bullet needed; the Assumptions section already pins it._
- [x] CHK033 - Is there an edge case covering "TopicId in `subscribed_topics` TOML that uses a topic name with whitespace, control characters, or quotes" — given FR-002 permits these per Clarifications Q2? Operators might be surprised; is the permissive rule cross-referenced from any operator-facing artifact (quickstart, contracts/node-config.toml.md examples)? [Coverage, Spec §FR-002] — _verified 2026-05-30: FR-002 explicitly permits them ("no additional character-class restrictions"), Clarifications Q2 round 1 anchors the decision, quickstart §9 common-pitfalls table surfaces the case-sensitivity pitfall (T1 ≠ t1) which is the most likely operator confusion. No additional edge-case bullet adds value beyond what FR-002's normative permissive clause already states._

## Ambiguity

- [x] CHK034 - Does the spec quantify what "after the mutating call has completed" means in FR-004's temporal clause (when interleaved with the receive task), or does it rely entirely on FR-015's linearizability definition? Is the cross-reference between FR-004 and FR-015 explicit enough? [Clarity, Spec §FR-004 + FR-015] — _resolved 2026-05-30: FR-004 grew an explicit citation "linearizable per FR-015 — the mutator's release happens-before any subsequent receive-path observation" so readers see the layering inline without having to find FR-015 themselves. Semantic content unchanged; precision tightened._
- [x] CHK035 - Is the term "linearizable" defined in the spec, or only referenced by name with the "global serial order" gloss in FR-015? Does FR-015's gloss stand alone, or does it implicitly assume the reader knows the database-theory concept? [Clarity, Spec §FR-015] — _verified 2026-05-30: FR-015's gloss ("behaves as if all operations execute in some global serial order consistent with each call's program order") plus the three concrete consequences listed (Added-vs-AlreadyPresent on concurrent subscribe, snapshot reflecting linearized order, etc.) make the term operationally self-contained for the technical audience. No need to cite Herlihy or external definitions._
- [x] CHK036 - Is "operator-visible" (used in SC-006 and quickstart.md) defined? It implicitly means "appears in the structured log stream at 001's default `info` level" — is that derivable from spec text alone or only by reading 001's contracts/cli.md? [Ambiguity, Spec §SC-006] — _verified 2026-05-30: SC-006 anchors the term explicitly: "operator-visible at default log-level configuration (the same default that surfaces 001 FR-010's warn-level drop events)". The chain to 001's `info` default is one hop and stated in SC-006 itself._
- [x] CHK037 - Is "snapshot" used with the same meaning across FR-006 (received_messages), FR-013 (subscriptions), and the test-discipline notes referring to `await_delivery`? All three flavors of "snapshot" mean "owned, stable, race-free against future mutations" — is this consistency stated anywhere or inferred? [Clarity, Spec §FR-006 vs §FR-013] — _verified 2026-05-30: consistent semantics (owned, stable, race-free) across all three usages. FR-013 explicitly says it "mirrors 001 FR-006's `received_messages()` contract", which anchors the consistency directly. `await_delivery` is the test-discipline primitive that ensures the snapshot observed post-mutation reflects post-mutation state; the implication that "snapshot" means the same thing in all three is structural._

## Non-Functional

- [x] CHK038 - Are FR-014's debug-level events recorded as deliberately invisible at default log level, with a forward note that operators wanting them must use `--log-level debug`? Is this constraint normatively in the spec/FR-014 or only in research.md §7 / contracts/library-api.md / quickstart.md? [Completeness, FR-014 vs supporting artifacts] — _verified 2026-05-30: FR-014 itself contains the normative statement "Debug-level is invisible at the `info` default established by 001's CLI contract but available with `--log-level debug` for replay / retry tracing." contracts/library-api.md and quickstart.md §7 echo it as operator-facing notes; spec carries the normative claim._
- [x] CHK039 - Is the "no I/O during subscribe/unsubscribe" requirement (FR-006 + FR-015) reflected as an enforceable / reviewable constraint, or only as English prose? Could a future contributor add a registry call to `subscribe()` without violating any explicit FR? [Measurability, FR-006] — _verified 2026-05-30: FR-006 normatively states "Neither call MUST perform any I/O, registry lookup, persistence, or external notification." Review-anchored — a code reviewer reading the impl can verify directly. Stronger enforcement (linter rules, type-level effect tracking) is out of v1 scope and would not buy much beyond what review already catches at this surface area._

## Dependencies & Assumptions

- [x] CHK040 - The spec Assumptions section says "Inherits all 001 assumptions". Is the inherited set enumerated explicitly somewhere (Trust, Liveness, Single-process scope, No cryptography, No failure handling, Configuration trust), or does the reader have to open 001's spec to know which? [Traceability, Spec §Assumptions] — _verified 2026-05-30: spec Assumptions section enumerates the six inherited 001 assumptions inline: "Trust assumption, liveness assumption, single-process scope, no failure handling, configuration trust, no cryptography (extended to this iteration as well)." Reader doesn't need to open 001's spec to know the set._
- [x] CHK041 - The plan's "no new dependency" claim is verifiable only by reading Cargo.toml. Is there a single explicit sentence somewhere (plan.md Technical Context, or research.md) of the form "002 introduces zero new external crates"? [Measurability, plan.md] — _verified 2026-05-30: plan.md Primary Dependencies section explicitly states "No new crate is required — the subscription set is `std::collections::HashSet`, the lock is `std::sync::Mutex`, the new TOML field rides inside the existing `serde::Deserialize` derive." Reviewable in one read._

## ADR Coverage

- [x] CHK042 - Is ADR 0008's scope (per research.md §8) bounded such that future failure-mode changes (Result-wrapping when registry validation lands) author a follow-on ADR rather than amend 0008? Is the "0008 covers X, not Y" boundary explicit in the ADR slot description? [Clarity, research.md §8] — _verified 2026-05-30: research.md §8 contains the explicit boundary: "Notes the forward-extension path: when failure modes appear (registry validation, persistence I/O), the return type becomes `Result<Outcome, Error>` — that's a follow-on ADR, not a revision of 0008." Future ADR authors have the trigger condition._
- [x] CHK043 - The four tactical decisions research.md §8 declines to ADR-ize (TopicId mirroring PeerId, the Arc-Mutex-HashSet primitive, Message envelope shape, TOML field extension) — is the "tactical vs structural" classification per Constitution Principle III justified in research.md §8 itself, or does the reader have to infer? [Traceability, research.md §8 vs Constitution Principle III] — _resolved 2026-05-30: research.md §8 grew a one-sentence Principle III citation immediately before the tactical-decisions list: "Per Constitution Principle III ('a decision is structural if reversing it would require touching unrelated code, external interfaces, or another protocol layer; a decision is tactical if reversing it is a local rewrite'), each of the below qualifies as tactical — reversing any of them is a local rewrite within pubsub-node/, with no ripple across protocol layers, external interfaces, or unrelated subsystems." The classification is now traceable in-line._

## Forward references

- [x] CHK044 - Are the trigger conditions for revisiting N-001 ("Node-facing REST/admin API") and N-002 ("connection-oriented network model — feature 004") in IMPLEMENTATION_NOTES.md worded such that they will be recognizably triggered when those features land? Does N-002's "feature 004" name match what ROADMAP.md §2 #004 calls it? [Consistency, IMPLEMENTATION_NOTES.md vs ROADMAP.md] — _verified 2026-05-30: N-002's trigger "feature 004 (connection-oriented network model)" matches ROADMAP §2 #004's heading "Connection-oriented network model" exactly. N-001's trigger ("when a Node-facing REST / admin API is introduced") is intentionally open-ended — no specific feature number is anchored in the roadmap for it; this is appropriate for an operator-demand-driven trigger rather than a known-feature trigger._
- [x] CHK045 - Are forward references to 003 (crypto), 004+ (connection-close on misbehavior), and 008 (registry / publish authority) consistently phrased across spec Assumptions, FR-008's "publish authority is registry's concern", FR-009's "forwarded copies", and research.md §9 "Open follow-ups"? [Consistency] — _verified 2026-05-30: forward references use feature number + brief descriptor consistently: "003 (message envelope / mock crypto)", "004+" or "feature 004 (connection-oriented network model)", "feature 008 (registry)", "(feature 012)" for on-chain. Phrasing matches across spec and research.md._

## Notes

- This is the **first pass** of the readiness review. Pass-2 typically surfaces cascade drifts from pass-1's resolutions (per ROADMAP.md §4 convergence rule); plan for at least one more pass after each substantive resolution.
- CHK017 was flagged by the user during /speckit-checklist invocation as a known concern; it stays in the checklist as the placeholder for the eventual rename decision (or for the explicit "keep the name with rationale" verdict).
- Severity / priority is not formally assigned in this pass; items reading like **[Conflict]** (CHK025) and **[Naming, Consistency]** (CHK017) are the highest-impact; **[Coverage]** items recording deliberate non-exercise (CHK028, CHK031) can resolve as "intentional, recorded as such" without code change.

